version: "1"
default_connection: dev
connections:
  dev:
    name: dev
    engine: mariadb
    host: localhost
    port: 3306
    user: root
    database: testdb
  local:
    name: local
    engine: sqlite
    path: /home/testuser/data/local.db
repos:
  nightly:
    path: /home/testuser/backups/nightly
